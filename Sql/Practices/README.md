This folder contains my daily SQL practice files, organized by topic.

To build strong query-writing skills and develop intuition for real-world analytics tasks.  
I practice different topics each day to cover the full SQL

-->>okay so far so from my practice
things i got developed from beginner in sql journey the stuggles which mades me better slightly better every other day are

When I started, the challenges are mostly frm my Conversion of a business question into SQL logic
Deciding when to use GROUP BY vs window functions
Understanding join behavior (especially LEFT JOIN edge cases)
Getting wrong results even when the query “worked” lol
Writing queries that were correct but hard to read
also....
Grouping incorrectly and getting inflated counts
Using subqueries where a window function was better
Forgetting how NULL values affect COUNT, AVG, and joins
Overcomplicating simple problems while learning all the concepts i tried the most recent topic to implement r mix them up
so ...
Over time, I changed how I approach problems -->Broke problems into smaller logical steps
Used CTEs to make queries easier to read and c where am messing around
trying to b Replaced of nested queries with cleaner patterns
